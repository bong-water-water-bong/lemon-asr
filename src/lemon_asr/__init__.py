"""lemon-asr — local-first ASR pipeline for YouTube + arbitrary audio.

A small toolkit that pairs an OpenAI-compatible faster-whisper server
with a Glasp-markdown ingester, so transcripts land in the same
txt/srt/json layout regardless of upstream source.
"""

__version__ = "0.1.0"
